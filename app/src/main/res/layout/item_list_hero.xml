<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="hero"
            type="n7.ad2.heroes.db.HeroModel" />
    </data>

    <android.support.v7.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="0dp"
        app:cardElevation="2dp"
        app:cardUseCompatPadding="true">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:foreground="?android:attr/selectableItemBackground"
                android:id="@+id/iv_item_list_hero"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:adjustViewBounds="true"
                android:background="?colorAccent"
                android:contentDescription="@string/setting_tell_friend_title"
                android:onClick="@{(view)->hero.startHeroFull(view)}"
                android:paddingStart="2dp"
                android:paddingEnd="0dp"
                android:scaleType="centerCrop"
                android:transitionName="iv"
                srcHolder="@{@drawable/hero_placeholder}"
                srcError="@{@drawable/hero_placeholder_error}"
                srcPath='@{"file:///android_asset/heroes/" + hero.codeName + "/full.webp"}'
                tools:ignore="UnusedAttribute"
                tools:src="@drawable/hero_placeholder" />

            <TextView
                android:id="@+id/tv_item_list_hero"
                android:layout_gravity="bottom"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginStart="2dp"
                android:background="@drawable/gradient"
                android:paddingStart="2dp"
                android:paddingEnd="0dp"
                android:text="@{hero.name}"
                android:textSize="10sp"
                android:textStyle="bold"
                tools:text="@sample/heroes.json/data/name"
                tools:ignore="SmallSp" />

        </FrameLayout>

    </android.support.v7.widget.CardView>
</layout>